#pragma once
#include <stack>
namespace JR_GeometryArray
{
	using namespace std;
	//GeometryArray-- an array that can not be larger then its specified size but keeps track of free indices
	class GeometryArray
	{
		
	};
}